module.exports = {
    theme: {
      extend: {
        fontFamily: {
          neueMontreal: ['NeueMontreal', 'sans'], // Fallback to 'sans' if the custom font fails to load
        },
      },
    },
    plugins: [],
  };